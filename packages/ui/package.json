{
  "name": "@agent-ui/ui",
  "version": "0.0.0",
  "description": "React + Vite + Tailwind dashboard. Reads only the core event schema.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
