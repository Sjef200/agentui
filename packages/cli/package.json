{
  "name": "@agent-ui/cli",
  "version": "0.0.0",
  "description": "agent-ui CLI: watch / ingest / replay / demo for AI coding agents",
  "type": "module",
  "license": "MIT",
  "bin": {
    "agent-ui": "./dist/index.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsup",
    "test": "vitest run",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@agent-ui/core": "workspace:*",
    "commander": "^12.1.0"
  }
}
