// Placeholder skeleton.
//
// The Codex adapter (Phase 7, T7.1) is verified LOCALLY against an installed
// `codex` binary. Run `codex --help`, confirm its machine-readable output, then
// implement a pure parser emitting normalized core events (agent="codex") with a
// golden-file test, and register it in the CLI adapter selection.
export const AGENT_NAME = 'codex' as const;
