# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to add a changeset describing a user-facing change; release
automation (Phase 10.2) consumes them to version and publish the public packages.
